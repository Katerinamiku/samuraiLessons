import React from 'react';
import preloader from '../../../common/pawsPreloader.gif';

const Preloader = () => {
    return (
        <div>
            <img src={preloader}/>
        </div>
    );
};

export default Preloader;
